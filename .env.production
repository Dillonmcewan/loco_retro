# Loaded by Vite during `vite build` (production mode). Committed so the prod
# build is reproducible from a clean checkout. Override locally with
# .env.production.local if needed.

# Placeholder until the sync Worker is deployed under our Cloudflare account.
# Update to the resulting `<worker-name>.<account>.workers.dev` host after the
# first `wrangler deploy`.
VITE_SYNC_HOST=loco-retro-sync.dillonmcewan.workers.dev
